class Api::V1::Trips::EventsController < ApplicationController  
  def index
    # trip = Trip.find(params[:id])
    # city = CityFacade.get_city_info(trip.city, trip.country, trip.postcode)
    # trip_poros = CityFacade.get_restaurant(city.place_id)
    
    # render json: TripSerializer(trip_events)
    # # make poros into models
    # # iterate through trip poros? 
    # # trips_poros.each do |poro|
    # #   TripEvent.create!(trip_id: trip.id, event_date: "something", event_time: "something", event_id: poro.place_id)
    # # end
  end
end